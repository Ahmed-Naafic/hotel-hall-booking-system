node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma
/logs

# Firebase service-account key — its contents are already extracted into
# .env (FIREBASE_PROJECT_ID/CLIENT_EMAIL/PRIVATE_KEY); never commit the raw
# download.
*firebase-adminsdk*.json

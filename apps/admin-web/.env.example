# Admin Web local environment template. Copy to `.env` for local development.
# (docs/03-standards/naming-conventions.md §10; Vite only exposes VITE_-prefixed vars.)

# Backend API base URL. Defaults to http://localhost:3000/api/v1 if unset.
VITE_API_BASE_URL=

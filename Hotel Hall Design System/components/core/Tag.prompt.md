Pill chip for amenities and filters; clickable when `onClick` is passed.

```jsx
<Tag selected onClick={toggle}>Harbour view</Tag>
<Tag icon={<Icon name="wifi" />}>Wi-Fi included</Tag>
```

Selected state is a teal 1.5px border with teal-100 fill. `onRemove` adds a dismiss ×.

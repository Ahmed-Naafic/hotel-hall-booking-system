# Scripts

Scripts that operate on the repository or environment as a whole (seeding, migrations, CI
helpers) — never business logic. Governed by `docs/02-architecture/folder-structure.md` §1,
§6.

**Status:** Placeholder. No scripts exist here yet.

This folder is reserved by the approved repository layout, created at repository
initialization per `docs/02-architecture/adr/0003-repository-initialization-scaffolding.md`.
Its contents (`seed/`, `migrate/`, `ci/`) are added as the backend and its database exist,
per `Development-Lifecycle.md` Phase 8.

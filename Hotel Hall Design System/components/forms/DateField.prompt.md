Check-in / check-out pair — the booking flow's primary control.

```jsx
<DateField checkIn="Aug 12" checkOut="Aug 15" nights={3} onClick={openCalendar} />
```

Sits inside the search bar on the homepage and docks under the header on room pages. Cosmetic here; wire a calendar in production.

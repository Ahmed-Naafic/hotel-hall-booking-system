Gold star rating for rooms, venue and reviews — the only filled icon in the system.

```jsx
<Rating value={4.5} count={218} />
```

Supports half stars. Pass `label={false}` to hide the numeral in tight card layouts.
